[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "idp-config"
version = "0.1.0"
dependencies = [
    "boto3",
]

[tool.setuptools]
packages = { find = {} }